namespace: oob-ns
subnetLabels:
  - key: dhcp
    value: "true"
  - key: key-2
    value: value-2
# leaseTimes is optional; both default to 24h when omitted.
leaseTimes:
  preferredLifetime: 24h
  validLifetime: 24h
